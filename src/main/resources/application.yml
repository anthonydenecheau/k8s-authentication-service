server:
  port: 8095
  contextPath: /auth

management:
  endpoints:
    web:
      base-path: /manage
  security:
    enabled: false
    
signing:
  key: 345345fsdfsf5345
  
#Setting the logging levels for the service
logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.scc: DEBUG
